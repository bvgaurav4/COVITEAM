import React from 'react';
import { BoxProps } from '@mantine/core';
interface HeaderControlsProps extends BoxProps {
    onSearch?: () => void;
    githubLink?: string;
    withDirectionToggle?: boolean;
    withSearch?: boolean;
    withGithub?: boolean;
    withDiscord?: boolean;
    discordLink?: string;
    withColorScheme?: boolean;
}
export declare function HeaderControls({ onSearch, githubLink, withDirectionToggle, withSearch, withGithub, withDiscord, withColorScheme, discordLink, ...others }: HeaderControlsProps): React.JSX.Element;
export {};
